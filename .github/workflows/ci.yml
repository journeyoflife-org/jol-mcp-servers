name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  lint-type-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        server:
          - jol_git_server
          - jol_jira_server
          - jol_compliance_server
          - jol_docs_server
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Lint (ruff)
        run: uv run ruff check shared/ servers/${{ matrix.server }}/

      - name: Type check (mypy)
        run: uv run mypy shared/ servers/${{ matrix.server }}/

      - name: Test
        run: uv run pytest shared/ servers/${{ matrix.server }}/tests/ -v --tb=short

  security-scan:
    runs-on: ubuntu-latest
    needs: lint-type-test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Bandit scan
        run: uv run bandit -r shared/ servers/ -ll

      - name: pip-audit
        run: uv run pip-audit
