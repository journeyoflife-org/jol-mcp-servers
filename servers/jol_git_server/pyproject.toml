[project]
name = "jol-git-server"
version = "0.1.0"
description = "MCP Git server — read-only repository inspection tools"
requires-python = ">=3.12"
license = { text = "Proprietary — All Rights Reserved" }
dependencies = [
    "mcp>=1.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pyjwt>=2.8",
    "gitpython>=3.1",
]
