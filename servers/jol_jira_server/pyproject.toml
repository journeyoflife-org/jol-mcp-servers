[project]
name = "jol-jira-server"
version = "0.1.0"
description = "MCP Jira server — issue search and create tools"
requires-python = ">=3.12"
license = { text = "Proprietary — All Rights Reserved" }
dependencies = [
    "mcp>=1.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pyjwt>=2.8",
    "httpx>=0.27",
]
